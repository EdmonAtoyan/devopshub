apiVersion: v1
kind: Service
metadata:
  name: media-service
  namespace: devops-hub
spec:
  selector:
    app: media-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
