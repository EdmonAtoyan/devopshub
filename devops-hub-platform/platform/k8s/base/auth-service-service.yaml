apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: devops-hub
spec:
  selector:
    app: auth-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
