apiVersion: v1
kind: Service
metadata:
  name: community-api
  namespace: devops-hub
spec:
  selector:
    app: community-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3001
  type: ClusterIP
