apiVersion: v1
kind: Service
metadata:
  name: devops-community-web
  namespace: devops-community
spec:
  selector:
    app: devops-community-web
  ports:
    - name: http
      port: 3000
      targetPort: http
